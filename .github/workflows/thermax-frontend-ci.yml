name: Thermax-Frontend Workflow

on:
  push:
    branches:
      - master

jobs:
  test_job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Print a message
        run: echo "Hello from the dummy GitHub Actions workflow!"
